ALTER TABLE users RENAME TO _users;
CREATE TABLE users(
  user_id VARCHAR(255) NOT NULL PRIMARY KEY UNIQUE,
  last_authenticated_at DATETIME DEFAULT NULL,
  permission_list TEXT DEFAULT "[]",
  is_disabled BOOLEAN DEFAULT 0 NOT NULL
);
INSERT INTO users (user_id, last_authenticated_at, permission_list, is_disabled) SELECT user_id, last_authenticated_at, entitlement_list, is_disabled FROM _users;
DROP TABLE _users;
